{
  "name": "@agent-control-tower/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.build.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@agent-control-tower/domain": "workspace:*",
    "@fastify/cors": "^11.0.1",
    "dotenv": "^17.4.1",
    "fastify": "^5.8.4",
    "undici": "^8.0.2"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsx": "^4.19.3",
    "typescript": "^6.0.2",
    "vitest": "^3.2.4"
  }
}
