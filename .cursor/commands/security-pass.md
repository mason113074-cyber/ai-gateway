# Command: security-pass

Audit the codebase (or the changed files) for:

- Secrets in code, tests, examples, or docs
- Auth bypass or missing checks
- Policy bypass
- Unsafe shelling or path traversal
- Unchecked external calls or input validation gaps

Report findings; do not auto-fix without confirmation for security-sensitive changes.
