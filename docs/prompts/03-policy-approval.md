Implement T003 and T004: policy evaluation and approval workflow skeleton.

Allowed paths:
- apps/api/**
- apps/web/**
- packages/domain/**
- docs/**

Definition of done:
- policy decisions can return allow / deny / requires_approval
- approval records can be created and updated
- pending approval queue is visible in UI
- risky actions are not silently allowed
