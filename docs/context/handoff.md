# Handoff

- **Latest working state**: This repo is the canonical root. Branch `chore/unify-repo-and-cursor-os` (or `main` after push). Cursor OS files at repo root.
- **Commands to run**: `pnpm install`, `pnpm build`, `pnpm test`, `pnpm dev` (web :3000, API :4000).
- **Files to inspect next**: AGENTS.md, docs/context/current-status.md, .cursor/rules/*, docs/backlog.md.
- **Unresolved risks**: None.
